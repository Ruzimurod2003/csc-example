#include <iostream>
#include<math.h>
using namespace std;
int main() {
	float x=1.02;
	float y=12;
	float z=13.11;
	float Natija=cos(sqrt(exp(x)/abs(x-y)))*(pow(tan(x-y),(1/7))/pow(sin(pow(x,(y-z))),(-1)));
    cout <<"Natija: "<<Natija;
    return 0;
}
